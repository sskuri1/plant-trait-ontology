pattern_name: to/ratio
classes: []

relations:
  has dividen quality: TO_has_dividend_quality
  has divisor quality: TO_has_divisor_quality
    
vars: 
  dividend: "'Thing'"
  divisor: "'Thing'"
  attribute: "'Thing'"

name:
  text: "%s to %s ratio"
  vars:
    - dividend
    - divisor

def: 
  text: "The proportion of %s in %s."
  vars:
    - dividend
    - divisor
    
equivalentTo: 
  text: "%s and 'has dividen quality' some %s and 'has divisor quality' some %s"
  vars:
    - attribute
    - dividend
    - divisor