pattern_name: to/response
classes:
  response: PATO_0000077

relations:
  part of: BFO_0000050
  inheres in: RO_0000052
  occurs in: BFO_0000066
    
vars: 
  environment: "'Thing'"

name:
  text: "response to %s"
  vars:
    - environment
    
annotations: 
  - property: oio:hasExactSynonym
    text: "%s"
    vars:
      - iri name

def: 
  text: "The response to %s."
  vars:
    - environment
    
equivalentTo: 
  text: "'response' and 'occurs in' some %s"
  vars:
    - environment